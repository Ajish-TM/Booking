import "@hotwired/turbo-rails"
import "@hotwired/stimulus"
import "@hotwired/stimulus-loading"
// Include Rails UJS if you have it installed as a separate package
import Rails from "@rails/ujs"
Rails.start()